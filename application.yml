spring:
  application:
    name: "configserver"
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/onrcanogul/springboot-microservices-config.git"

server:
  port: 8071
